<nav class="navbar navbar-default navbar-fixed-top navbar-wagon" role="navigation">
    <div class="container-fluid">
      <!-- Brand and toggle get grouped for better mobile display -->
      <div class="navbar-header">
        <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1">
          <span class="sr-only">Toggle navigation</span>
          <span class="icon-bar"></span>
          <span class="icon-bar"></span>
          <span class="icon-bar"></span>
        </button>
        <div class="row list-inline">
        <li><a class="navbar-brand" href="/">
          <%= image_tag "ghost.png" %>
        </a></li>
          <li><h1 id="haunted_house">HauntedBnb</h1></li>
        </div>
      </div>


      <!-- Collect the nav links, forms, and other content for toggling -->
      <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
        <ul class="nav navbar-nav navbar-right list-inline">
                <% if user_signed_in? %>

                    <li>
                  <%= link_to flats_path do %><i class="fa fa-bed"></i> All the flats
                    <% end %></li>



                  <li class="dropdown"><%= link_to "#", {class: "dropdown-toggle", "data-toggle" => "dropdown", "role" => "button", "aria-expanded" => "false"} do %>
                  <% if current_user.picture %>
                   <%= image_tag current_user.picture, class: "img-avatar" %>
                   <% else %>
                   <%= image_tag "http://lorempixel.com/30/30/people", class: "img-avatar" %>
                   <% end %>
                   <% if current_user.first_name %>
                  <%= current_user.first_name %>
                  <% else %>

                  <%= current_user.email %>
                  <% end %>
                   <span class="caret"></span>
                  <% end %>
                  <ul class="dropdown-menu" role="menu">
                    <li><%= link_to owner_flats_path(current_user) do %><i class="fa fa-bed"></i> My Flats<% end %></li>
                 <!--  <% if current_user.flats.first %>
                    <li><%= link_to owner_flats_path(current_user) do %><i class="fa fa-cart-plus"></i> Bookings for my flats<% end %></li>
                  <% end %> -->
                    <li><%= link_to destroy_user_session_path, method: 'delete' do %><i class="fa fa-sign-out"></i> Sign out<% end %></li>
                  </ul>
                  </li>
                <% else %>
                  <li><%= link_to 'Sign Up', new_user_registration_path, class: "btn" %></li>
                  <li><%= link_to 'Log In', new_user_session_path, class: "btn" %></li>

              <!-- Facebook login -->
                 <li>
                  <%= link_to user_omniauth_authorize_path(:facebook), class: "btn" do %>
                  Connect with <i class="fa fa-facebook-square"></i>
                  <% end %>
                </li>
                <% end %>

          <li>
            <%= link_to "List your space", new_flat_path, class: "btn" %>
          </li>
        </ul>
      </div><!-- /.navbar-collapse -->
    </div><!-- /.container-fluid -->
  </nav>